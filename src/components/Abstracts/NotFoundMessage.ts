import styled from "styled-components";

export default styled.span`
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 1.2rem;
    max-width: 100%;    
    text-align: center;
`